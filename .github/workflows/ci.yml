name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

jobs:
  check:
    name: Test and lint
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2

      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Clippy
        run: cargo clippy --workspace --all-targets -- -D warnings

      - name: Test
        run: cargo test --workspace

      # Building the app crate needs the sidecar to exist, even though the
      # installer itself is only built on release.
      - name: Stage the CLI sidecar
        run: |
          cargo build --release -p msw-cli
          New-Item -ItemType Directory -Force -Path msw-app/binaries | Out-Null
          Copy-Item target/release/msw.exe msw-app/binaries/msw-x86_64-pc-windows-msvc.exe

      - name: Build
        run: cargo build --workspace --release
